<div class="container rooms">
  <div class="row">
    <div class="col-sm-12">
      <header>
      </header>

    <% @artists.each do |x| %>
     <div class="col-sm-3">
       <div class="panel panel-default">
          <div class="panel-body">
           
           <img src="<%= x.image_url %>" id="imagex">
           <div class="details clearfix" style="text-align: center;"><h3><%= x.name %></h3></div>
           <div class="actions text-right"><%= link_to "Ver Más", x , class: "btn btn-primary btn-lg btn-block" %></div><br>
           
         </div>
       </div>
     </div>
     <% end %>
    </div>
  </div>
</div>

          

